# ИИ-Агент СМК — актуальная документация проекта

Документ описывает текущее состояние проекта, его основные сценарии, новые функции, изменения в архитектуре и состав модулей.

**Дата актуализации:** 2026-04-05
**Источник правды по реализации:** [`main.py`](main.py), [`global_settings.json`](global_settings.json), [`local_settings.json`](local_settings.json)

---

## 1) Краткое описание проекта

**ИИ-Агент СМК** — Windows desktop-приложение на базе `CustomTkinter`, которое помогает работать с документами и артефактами системы менеджмента качества:

- выполнять консультации и аудит по документам СМК;
- искать релевантные фрагменты по базе знаний через RAG;
- читать Word, Excel, PDF, GraphML и legacy-документы;
- редактировать Word по индексам абзацев;
- искать и обновлять записи в Excel-журналах;
- генерировать новые документы Word и Excel;
- сохранять подтвержденные факты в долговременную память;
- восстанавливать старый контекст диалога из архивной памяти;
- готовить черновики email и встреч в Outlook;
- выполнять OCR-распознавание PDF-сканов через Vision-модели;
- сохранять и загружать сессии диалогов;
- работать в двух режимах доступа (Admin/Guest);
- индексировать сетевые папки и исключать файлы по ключевым словам;
- формировать кликабельные ссылки на файлы в ответах;
- искать информацию в интернете через Tavily API;
- искать определения и факты в Википедии;
- транскрибировать аудиофайлы (.mp3, .wav, .m4a, .ogg) через голосовые модели;
- навигировать по файловой базе через умный поиск файлов по категориям и ключевым словам;
- настраивать аудио-провайдеры (OpenRouter/Groq) и SOCKS5-прокси.

Ключевая идея проекта — **агентный цикл с Tool Calling**, где LLM не только отвечает текстом, но и сама выбирает нужные инструменты, выполняет действия и возвращает итог пользователю.

**Технологический стек:**
- GUI: customtkinter
- Векторная база: ChromaDB
- Работа с PDF: PyMuPDF (fitz)
- LLM API: OpenRouter (с поддержкой Vision- и Audio-моделей)
- XML/схемы: xml.etree.ElementTree
- Аудио: ffmpeg (ffprobe/ffmpeg для нарезки аудио)

---

## 2) Что изменилось в текущей версии

Ниже перечислены наиболее важные новые функции и архитектурные изменения относительно более ранней, упрощенной версии проекта.

### 2.1 Новые и расширенные функции

1. **Изоляция профиля пользователя**
   - Функция [`get_local_path()`](main.py:76) возвращает путь к изолированной папке профиля для конкретного экземпляра Агента.
   - Функция [`get_db_path()`](main.py:93) обеспечивает теневую репликацию: стягивает серверную БД на SSD пользователя для быстрой и безопасной работы.

2. **Vault-шифрование (AES-256)**
   - Функции [`get_vault_data()`](main.py:123) и [`save_vault_data()`](main.py:150) обеспечивают безопасное хранение чувствительных данных (API-ключей) с использованием шифрования Fernet.

3. **Динамические эмбеддинги**
   - Функция [`get_cloud_ef()`](main.py:171) реализует динамическую функцию эмбеддингов без глобальной инициализации, позволяя переключаться между моделями без перезапуска.

4. **Поиск в интернете через Tavily API**
   - Инструмент [`web_search_tavily()`](main.py:1112) выполняет поиск информации, новостей и актуальных требований во всем интернете. Возвращает до 5 источников с полным текстом и прямыми ссылками. Требует API-ключ в [`secrets.vault`](secrets.vault) (поле `tavily_key`).

5. **Поиск в Википедии**
   - Инструмент [`search_wikipedia_tool()`](main.py:1143) ищет термины, общие знания и определения в Википедии. Возвращает заголовок, краткое описание (до 2500 символов) и ссылку на статью.

6. **Интеллектуальный выбор источника поиска**
   - Системный промпт агента обновлен: теперь при отсутствии информации в локальной базе знаний агент предлагает пользователю выбрать источник (интернет или Википедия) и выполняет поиск только после явного согласия.

7. **Единая RAG-база по документам и памяти**
   - Индексация теперь строится вокруг коллекции `smk_docs` в ChromaDB, куда попадают документы из каталогов `SMK_Docs` и `Memory`.

8. **Инкрементальная переиндексация файлов**
   - Добавлен механизм сравнения временных меток файлов через [`sync_vector_db()`](main.py:939) и файл состояния [`file_states.json`](file_states.json). Переиндексируются только новые, измененные или удаленные файлы.

9. **Поддержка legacy-форматов `.doc` и `.rtf`**
   - Функция [`convert_legacy_to_docx()`](main.py:663) реализует COM-конвертацию Word-документов в `.docx` с сохранением результата в кэш `.cache/`, после чего текст читается обычным способом.

10. **Поддержка схем `.graphml`**
    - Функция [`extract_text_from_graphml()`](main.py:403) парсит схемы yEd через извлечение узлов, потоков, групп и маршрутизации в плоское текстовое описание для RAG.

11. **Умная работа с Excel**
    - Появились два специализированных инструмента:
    - [`smart_excel_search()`](main.py:1526) — поиск релевантных строк через LLM-разведку структуры листа и правил поиска;
    - [`smart_excel_edit()`](main.py:1634) — безопасное обновление или добавление строк в копию файла `*_Правки.xlsx`.

12. **Генерация документов с нуля**
    - Добавлены:
    - [`generate_document_from_scratch()`](main.py:1863) для Word-документов;
    - [`generate_excel_from_scratch()`](main.py:1969) для Excel-таблиц.

13. **Генерация документов по шаблону**
    - Инструмент [`generate_document_from_template()`](main.py:1769) создает новый документ на базе существующего шаблона с интеллектуальной заменой содержимого.

14. **Долговременная память и забывание фактов**
    - Появились функции:
    - [`memorize_important_fact()`](main.py:1157) — запись фактов в [`Memory/agent_memory.md`](Memory/agent_memory.md);
    - [`forget_fact()`](main.py:1170) — удаление фактов из памяти с последующей переиндексацией.

15. **Архив вытесненного контекста диалога**
    - Старые пары сообщений сохраняются в коллекцию `temp_chat_memory`, а затем могут быть извлечены через [`recall_past_conversation()`](main.py:1076).

16. **Фоновый "секретарь"**
    - Введен асинхронный анализ последних сообщений через [`run_background_secretary()`](main.py:2752), который автоматически сохраняет подтвержденные факты из диалога.

17. **Outlook-интеграция**
    - Добавлены инструменты:
    - [`draft_email_tool()`](main.py:1734) — черновик письма;
    - [`draft_meeting_tool()`](main.py:1750) — черновик встречи.

18. **Vision-роутинг для PDF и изображений**
    - Умный роутер [`extract_smart_vision_and_pdf()`](main.py:228) автоматически определяет, когда нужен OCR (мало текста, много изображений, схемы), и использует Vision-модель для распознавания. Включает mtime-кэширование.

19. **Настройки моделей через GUI**
    - Пользователь может менять основную модель, модель секретаря и модель эмбеддингов через окно настроек, открываемое из интерфейса.
    - Функция [`fetch_free_models()`](main.py:2293) загружает список бесплатных моделей из OpenRouter.

20. **Система сессий и история диалогов**
    - Реализовано автосохранение сессий чата в каталог [`Sessions/`](Sessions/) в формате JSON. Каждая сессия содержит уникальный ID, заголовок (генерируется автоматически через LLM), временную метку, историю сообщений и отображаемый текст.
    - Функция [`generate_session_title_background()`](main.py:2598) генерирует заголовки сессий в фоновом режиме.
    - Добавлена кнопка «История диалогов» в боковой панели для просмотра, загрузки и удаления старых сессий через [`open_history_window()`](main.py:2630).

21. **Система ролей (Admin/Guest)**
    - Введена двухуровневая система доступа:
    - **Admin** — полный доступ: сохранение сессий, запись фактов в память, архивация чата в векторную базу;
    - **Guest** — ограниченный доступ: данные не сохраняются на диск, факты не записываются в память.
    - Аутентификация выполняется по паролю через [`prompt_auth()`](main.py:2313), заданному в [`secrets.vault`](secrets.vault) (поле `admin_password`).

22. **Расширенная конфигурация индексации**
    - Добавлены новые параметры в [`global_settings.json`](global_settings.json):
    - `indexed_folders` — массив путей к папкам для индексации (поддержка сетевых путей, например `Z:/...`);
    - `exclude_keywords` — массив ключевых слов для исключения файлов из индексации (например, «архив», «not_index», «черновик»);
    - `audio_provider` / `audio_model` — настройки аудио-транскрибации;
    - `audio_chunk_mins` / `audio_overlap_secs` — параметры нарезки аудио.

23. **Кликабельные ссылки на файлы в ответах агента**
    - Агент теперь автоматически формирует ссылки на упомянутые документы в формате `[Из файла: Имя_файла.ext]`. При клике на ссылку файл открывается в системном приложении через [`on_link_click()`](main.py:2553).

24. **Улучшенный GUI**
    - Многострочное текстовое поле ввода (3-4 строки) с поддержкой Shift+Enter для переноса строки;
    - Стили бейджей (плашек) для сообщений пользователя и агента;
    - Прогресс-бар синхронизации базы знаний через [`update_progress_ui()`](main.py:2368);
    - Кнопка «Войти как Админ» с аутентификацией по паролю;
    - Диалог сохранения при генерации документов через [`ask_save_path_sync()`](main.py:2342).

25. **Генерация диаграмм Mermaid и yEd**
    - Добавлены инструменты [`generate_mermaid_diagram()`](main.py:1185) и [`generate_yed_diagram()`](main.py:1347) для создания визуальных схем по описанию. Позволяют генерировать блок-схемы, процессные диаграммы и графики в форматах Mermaid HTML и yEd GraphML.

26. **Поддержка HTML-диаграмм**
    - Функция [`extract_text_from_html_diagram()`](main.py:523) извлекает текстовое содержимое из HTML-файлов диаграмм для последующей индексации в RAG.

27. **Единый локатор файлов**
    - Функция [`find_target_file()`](main.py:690) выполняет интеллектуальный поиск файлов с учетом черного списка ключевых слов и настроек индексации, обеспечивая точное нахождение целевых документов.

28. **Сканирование папок для документов**
    - Функция [`scan_folders_for_docs()`](main.py:808) рекурсивно собирает список файлов из указанных папок, исключая файлы по ключевым словам, и возвращает готовый список для индексации.

29. **Управление состояниями файлов**
    - Функции [`get_file_states()`](main.py:846) и [`save_file_states()`](main.py:854) обеспечивают механизм инкрементальной индексации, отслеживая изменения файлов через временные метки и сохраняя состояние в [`file_states.json`](file_states.json).

30. **Ручная синхронизация**
    - Функция [`manual_sync()`](main.py:2809) позволяет вручную запустить полную синхронизацию векторной базы через интерфейс.

31. **Рендер Unicode-таблиц**
    - Функция [`generate_unicode_table()`](main.py:2385) обеспечивает красивое отображение табличных данных в чате.

32. **Markdown-обработка**
    - Функция [`apply_markdown()`](main.py:2473) обрабатывает markdown-разметку в ответах агента (жирный текст, курсив, заголовки).

33. **Транскрибация аудиофайлов**
    - Функция [`transcribe_audio_logic()`](main.py:540) выполняет расшифровку аудиофайлов (.mp3, .wav, .m4a, .ogg) через нарезку на фрагменты и отправку в голосовую модель (OpenRouter или Groq). Результат сохраняется в кэш и в Word-документ с последующей автоматической индексацией.

34. **Навигатор по файлам (list_available_files)**
    - Функция [`list_available_files()`](main.py:860) выполняет умный поиск и группировку проиндексированных файлов из `file_states.json` по категориям (аудио, Excel, Word, PDF, изображения, текст, схемы) с фильтрацией по ключевому слову.

35. **Настройки аудио и сети**
    - В окне настроек появилась вкладка «Аудио и Сеть» (только для Admin), где можно выбрать:
    - аудио-провайдер (OpenRouter/Groq);
    - аудио-модель (с автозагрузкой доступных моделей);
    - длину куска аудио (мин) и перекрытие (сек);
    - использование SOCKS5-прокси (host/port).

36. **Вкладка «Безопасность»**
    - В окне настроек появилась отдельная вкладка для управления API-ключами (OpenRouter, Groq, Tavily) и паролем администратора через зашифрованный Vault.

37. **Вкладка «О программе»**
    - В окне настроек добавлена вкладка с информацией о версии, разработчике и описании приложения.

38. **Улучшенная потоковая генерация ответов**
    - Агентный цикл [`App.agent_loop()`](main.py:3500) теперь использует потоковую (stream) генерацию ответов, что обеспечивает плавное отображение текста в чате по мере генерации.

39. **Удаление сессий из истории**
    - В окне истории диалогов добавлена кнопка удаления сессий с полной очисткой из файловой системы и векторной базы.

### 2.2 Архитектурные изменения

1. **Система настроек с вкладками**
   - Окно настроек [`App.open_settings()`](main.py:2824) теперь организовано по вкладкам:
   - «Модели» — выбор основной, Vision, секретаря и эмбеддинг-моделей;
   - «Исключения» — управление ключевыми словами для исключения из индексации;
   - «Папки» — интерактивное управление папками для индексации (добавление/удаление);
   - «Безопасность» (только Admin) — API-ключи и пароль администратора;
   - «Аудио и Сеть» (только Admin) — аудио-провайдер, модель, параметры нарезки, прокси;
   - «О программе» — информация о версии и разработчике.

2. **Разделение настроек на локальные и глобальные**
   - [`local_settings.json`](local_settings.json) — хранит пользовательские настройки (модели, история моделей, прокси);
   - [`global_settings.json`](global_settings.json) — хранит общие настройки (Vision, секретарь, эмбеддинги, аудио, папки, исключения);
   - [`secrets.vault`](secrets.vault) — зашифрованное хранилище API-ключей (OpenRouter, Groq, Tavily, admin_password).

3. **Переход к более явной агентной архитектуре**
   - Центральная логика сосредоточена в агентном цикле [`App.agent_loop()`](main.py:3500), где системная инструкция задает строгий порядок вызова инструментов.

4. **Разделение памяти на три уровня**
   - оперативная история чата — [`chat_history.json`](chat_history.json);
   - долговременные факты — [`Memory/agent_memory.md`](Memory/agent_memory.md);
   - архив вытесненного диалога — коллекция `temp_chat_memory`.

5. **Появление кэш-слоя для промежуточных преобразований**
   - Каталог `.cache/` используется для:
   - конвертированных legacy-документов (`{name}_converted.docx`);
   - markdown-копий распознанных схем GraphML (`{name}_schema.md`);
   - результатов Vision/OCR-распознавания PDF-сканов (`{filename}_vision.md`);
   - расшифровок аудиофайлов (`{name}_audio.md`).

6. **Инкрементальная индексация вместо полного сканирования "вслепую"**
   - Архитектура индексации учитывает измененные и удаленные файлы, снижая стоимость и время синхронизации.

7. **Многопоточность для тяжелых операций**
   - Все тяжелые операции (парсинг, API-вызовы, эмбеддинг, транскрибация) выполняются в отдельных потоках через `threading.Thread`:
   - синхронизация БД при старте приложения;
   - переиндексация после генерации документов;
   - агентный цикл обработки сообщений;
   - фоновый анализ диалога секретарем;
   - транскрибация аудиофайлов.
   - Обновление GUI выполняется через `self.after(0, lambda: ...)`.

8. **Система сессий с автосохранением**
   - Реализован четырехуровневый подход к хранению данных:
   - оперативная история чата — [`chat_history.json`](chat_history.json) (глобальная);
   - сессии диалогов — [`Sessions/`](Sessions/) (JSON-файлы с уникальным ID);
   - долговременные факты — [`Memory/agent_memory.md`](Memory/agent_memory.md);
   - архив вытесненного диалога — коллекция `temp_chat_memory` (только для Admin).

9. **Разделение доступа на основе ролей**
   - Введена система аутентификации с двумя ролями:
   - **Admin** — может сохранять сессии, записывать факты в память, архивировать чат;
   - **Guest** — работает в режиме «только чтение», данные не сохраняются на диск.
   - Пароль задается в [`secrets.vault`](secrets.vault), аутентификация через GUI.

10. **Расширенная индексация с поддержкой сетевых путей и исключений**
    - Функция [`sync_vector_db()`](main.py:939) теперь:
    - сканирует не только локальные папки, но и сетевые пути (например, `Z:/...`);
    - исключает файлы по ключевым словам из `exclude_keywords` (например, «архив», «not_index»);
    - использует расширенный список папок из `indexed_folders` в [`global_settings.json`](global_settings.json).

11. **Добавление слоя веб-поиска**
    - В архитектуру добавлен новый слой инструментов для поиска информации за пределами локальной базы знаний:
    - [`web_search_tavily()`](main.py:1112) — поиск в интернете через Tavily API;
    - [`search_wikipedia_tool()`](main.py:1143) — поиск в Википедии.
    - Инструменты интегрированы в агентный цикл с интеллектуальным выбором источника поиска.

12. **Изоляция профиля и теневые реплики**
    - Функции [`get_local_path()`](main.py:76) и [`get_db_path()`](main.py:93) обеспечивают работу нескольких экземпляров Агента на одном ПК с изолированными данными.
    - Vault-шифрование через Fernet защищает чувствительные данные.

---

## 3) Архитектура проекта

```mermaid
graph TB
    UI[CustomTkinter GUI] --> AGENT[Agent Loop]
    UI --> SETTINGS[Settings Window]
    UI --> HISTORY[History Window]
    UI --> AUTH[Auth System]
    AGENT --> TOOLS[Tool Layer]
    AGENT --> LLM[OpenRouter LLM]
    AGENT --> SECRETARY[Background Secretary]
    TOOLS --> WORD[Word Tools]
    TOOLS --> EXCEL[Excel Tools]
    TOOLS --> FILES[File Readers]
    TOOLS --> OUTLOOK[Outlook COM]
    TOOLS --> WEB[Web Search Tools]
    TOOLS --> AUDIO[Audio Transcription]
    TOOLS --> NAV[File Navigator]
    FILES --> CACHE[.cache]
    FILES --> DOCS[SMK_Docs]
    FILES --> MEMORY[Memory]
    FILES --> NETWORK[Network Paths]
    TOOLS --> VDB[(ChromaDB: smk_docs)]
    AGENT --> CHAT[chat_history.json]
    AGENT --> SESSIONS[Sessions/]
    AGENT --> ARCHIVE[(ChromaDB: temp_chat_memory)]
    SECRETARY --> MEMORYFILE[Memory/agent_memory.md]
    AUTH --> ADMIN[Admin Role]
    AUTH --> GUEST[Guest Role]
    WEB --> TAVILY[Tavily API]
    WEB --> WIKIPEDIA[Wikipedia API]
    UI --> PROFILE[Profile Isolation]
    PROFILE --> LOCAL[get_local_path]
    PROFILE --> SHADOW[get_db_path (Shadow Replica)]
    UI --> VAULT[Encrypted Vault]
    VAULT --> ENC[Fernet AES-256]
    AUDIO --> FFMPEG[ffmpeg/ffprobe]
    AUDIO --> GROQ[Groq API]
    SETTINGS --> LOCAL_CFG[local_settings.json]
    SETTINGS --> GLOBAL_CFG[global_settings.json]
```

### 3.1 Основные слои

#### GUI-слой

Интерфейс приложения реализован в классе [`App`](main.py:2170) и включает:

- чат с поддержкой markdown-разметки и кликабельных ссылок ([`apply_markdown()`](main.py:2473), [`on_link_click()`](main.py:2553));
- многострочное текстовое поле ввода (3-4 строки) с поддержкой Shift+Enter;
- кнопку синхронизации базы с прогресс-баром ([`update_progress_ui()`](main.py:2368));
- кнопку очистки чата;
- кнопку «История диалогов» для просмотра, загрузки и удаления старых сессий ([`open_history_window()`](main.py:2630));
- кнопку «Войти как Админ» с аутентификацией по паролю ([`prompt_auth()`](main.py:2313));
- окно настроек с вкладками ([`open_settings()`](main.py:2824));
- рендер Unicode-таблиц ([`generate_unicode_table()`](main.py:2385));
- стили бейджей (плашек) для сообщений пользователя и агента;
- диалог сохранения при генерации документов ([`ask_save_path_sync()`](main.py:2342)).

#### Агентный слой

Агентный цикл реализован в [`App.agent_loop()`](main.py:3500). Он:

- формирует системный промпт со строгим алгоритмом действий (9 шагов + шаги 1.1–1.4 для веб-поиска, аудио и навигации);
- передает модели схему доступных инструментов ([`get_tools_schema()`](main.py:3120));
- обрабатывает `tool_calls` ([`execute_tool()`](main.py:3453));
- ограничивает цикл 10 шагами;
- архивирует старые сообщения после переполнения окна истории (только для Admin);
- генерирует кликабельные ссылки на файлы в формате `[Из файла: Имя_файла.ext]`;
- использует потоковую (stream) генерацию ответов для плавного отображения текста.

#### Слой инструментов

Инструменты объявляются в [`App.get_tools_schema()`](main.py:3120) и маршрутизируются через [`App.execute_tool()`](main.py:3453).

#### Слой веб-поиска

Добавлены инструменты для поиска информации за пределами локальной базы знаний:
- [`web_search_tavily()`](main.py:1112) — поиск в интернете через Tavily API;
- [`search_wikipedia_tool()`](main.py:1143) — поиск в Википедии.

#### Слой знаний и индексации

Индексация строится вокруг функции [`sync_vector_db()`](main.py:939), которая:

- сканирует папки из `indexed_folders` в [`global_settings.json`](global_settings.json) (включая сетевые пути);
- исключает файлы по ключевым словам из `exclude_keywords` (например, «архив», «not_index»);
- пропускает каталог `.cache`;
- читает поддерживаемые форматы;
- разбивает текст на чанки через [`chunk_text()`](main.py:799);
- сохраняет чанки в коллекцию `smk_docs`;
- удаляет записи для исчезнувших файлов.

#### Слой памяти

Память состоит из нескольких механизмов:

- история текущего чата сохраняется через [`App.save_history()`](main.py:2592);
- сессии диалогов автосохраняются в [`Sessions/`](Sessions/) через [`App.save_current_session()`](main.py:2616);
- заголовки сессий генерируются автоматически через [`App.generate_session_title_background()`](main.py:2598);
- подтвержденные факты пишутся в [`Memory/agent_memory.md`](Memory/agent_memory.md) (только для Admin);
- старые пары сообщений архивируются в `temp_chat_memory` в блоке переполнения истории внутри [`App.agent_loop()`](main.py:3500) (только для Admin).

#### Слой изоляции профиля

Слой обеспечивает:
- [`get_local_path()`](main.py:76) — изоляция данных каждого экземпляра Агента;
- [`get_db_path()`](main.py:93) — теневая репликация БД на локальный SSD;
- [`get_vault_data()`](main.py:123) / [`save_vault_data()`](main.py:150) — шифрование чувствительных данных через Fernet.

---

## 4) Структура репозитория

Ключевые файлы и каталоги:

- [`main.py`](main.py) — основная логика GUI, агентного цикла, индексации и инструментов;
- [`PROJECT_OVERVIEW.md`](PROJECT_OVERVIEW.md) — актуальная документация проекта;
- [`.env`](.env) — переменные окружения, включая `OPENROUTER_API_KEY`;
- [`global_settings.json`](global_settings.json) — настройки моделей, Excel-параметров, аудио и batch-индексации;
- [`local_settings.json`](local_settings.json) — локальные настройки пользователя (модели, прокси);
- [`secrets.vault`](secrets.vault) — зашифрованный Vault для API-ключей;
- [`start.bat`](start.bat) — запуск приложения в Windows;
- [`Установить Агента СМК на ПК.bat`](Установить Агента СМК на ПК.bat) — скрипт установки на ПК;
- [`Деплой на сервер.bat`](Деплой на сервер.bat) — скрипт деплоя на сервер;
- [`build_agent.bat`](build_agent.bat) — скрипт сборки приложения (PyInstaller);
- [`SMK_Docs/`](SMK_Docs/) — основное хранилище рабочих документов СМК;
- [`.cache/`](.cache/) — кэш конвертации и промежуточных текстовых артефактов;
- [`Memory/`](Memory/) — долговременная память агента;
- [`Memory/agent_memory.md`](Memory/agent_memory.md) — файл фактов;
- [`smk_vector_db/`](smk_vector_db/) — локальная база ChromaDB;
- [`file_states.json`](file_states.json) — состояние файлов для инкрементальной индексации;
- [`Sessions/`](Sessions/) — каталог с сохраненными сессиями диалогов (JSON-файлы).

---

## 5) Поддерживаемые форматы и источники данных

Функция [`read_local_file()`](main.py:735) поддерживает:

- `.txt`
- `.md`
- `.docx`
- `.doc`
- `.rtf`
- `.pdf`
- `.xlsx`
- `.xls`
- `.graphml`
- `.html`
- `.mp3`
- `.wav`
- `.m4a`
- `.ogg`

Дополнительно:

- если передать путь к папке, инструмент возвращает список доступных файлов, а не пытается читать директорию как документ;
- поиск файла выполняется в текущем пути, а затем рекурсивно внутри `SMK_Docs` и `Memory`;
- для `.doc/.rtf` используется промежуточная COM-конвертация;
- для `.graphml` создается текстовое описание, пригодное для RAG;
- для аудиофайлов проверяется наличие кэша расшифровки, иначе предлагается запустить транскрибацию.

### 5.1 Утилиты обработки документов

Проект включает ряд внутренних утилит для обработки документов:

| Функция | Описание | Строка |
|---------|----------|--------|
| [`get_base_path()`](main.py:58) | Возвращает абсолютный путь к серверной папке с поддержкой флага --server | :58 |
| [`get_local_path()`](main.py:76) | Возвращает путь к изолированной папке профиля | :76 |
| [`get_db_path()`](main.py:93) | Теневая репликация БД на SSD пользователя | :93 |
| [`get_vault_data()`](main.py:123) | Чтение зашифрованного Vault с fallback на .env | :123 |
| [`save_vault_data()`](main.py:150) | Сохранение зашифрованного Vault | :150 |
| [`get_llm_client()`](main.py:165) | Динамический клиент LLM без глобальной инициализации | :165 |
| [`get_cloud_ef()`](main.py:171) | Динамическая функция эмбеддингов без глобальной инициализации | :171 |
| [`get_all_paragraphs()`](main.py:187) | Собирает все абзацы документа (включая таблицы) в единый плоский список | :187 |
| [`read_docx_with_indices()`](main.py:197) | Читает docx и возвращает текст с пронумерованными абзацами [0], [1], [2]... | :197 |
| [`extract_text_from_pdf()`](main.py:210) | Извлекает текст из PDF с текстовым слоем | :210 |
| [`extract_smart_vision_and_pdf()`](main.py:228) | Умный Vision-роутер v1.2: автоопределение PDF/изображение → OCR или нативный текст, с mtime-кэшем | :228 |
| [`extract_text_from_excel_for_rag()`](main.py:350) | Конвертирует Excel в плоский текст для RAG | :350 |
| [`extract_text_from_graphml()`](main.py:403) | Парсит yEd .graphml: узлы, потоки, группы, маршрутизация | :403 |
| [`extract_text_from_html_diagram()`](main.py:523) | Извлечение текста из HTML-диаграмм | :523 |
| [`transcribe_audio_logic()`](main.py:540) | Транскрибация аудиофайлов через нарезку и голосовую модель | :540 |
| [`convert_legacy_to_docx()`](main.py:663) | Конвертирует .doc/.rtf в .docx через COM | :663 |
| [`find_target_file()`](main.py:690) | Интеллектуальный поиск файлов с учетом черного списка | :690 |
| [`read_local_file()`](main.py:735) | Универсальный ридер: все поддерживаемые форматы + список файлов в папке | :735 |
| [`chunk_text()`](main.py:799) | Разбивает текст на чанки для индексации | :799 |
| [`scan_folders_for_docs()`](main.py:808) | Рекурсивное сканирование папок для индексации | :808 |
| [`get_file_states()`](main.py:846) | Получение состояния файлов для инкрементальной индексации | :846 |
| [`save_file_states()`](main.py:854) | Сохранение состояния файлов | :854 |
| [`list_available_files()`](main.py:860) | Умный навигатор по файлам с группировкой по категориям | :860 |
| [`sync_vector_db()`](main.py:939) | Синхронизация векторной базы данных | :939 |
| [`recall_past_conversation()`](main.py:1076) | Поиск по архиву вытесненного диалога | :1076 |
| [`search_smk_knowledge_base()`](main.py:1093) | Семантический поиск по коллекции smk_docs | :1093 |
| [`web_search_tavily()`](main.py:1112) | Поиск в интернете через Tavily API | :1112 |
| [`search_wikipedia_tool()`](main.py:1143) | Поиск в Википедии | :1143 |
| [`memorize_important_fact()`](main.py:1157) | Запись факта в долговременную память | :1157 |
| [`forget_fact()`](main.py:1170) | Удаление факта из памяти | :1170 |
| [`generate_mermaid_diagram()`](main.py:1185) | Генерация диаграмм Mermaid HTML | :1185 |
| [`generate_yed_diagram()`](main.py:1347) | Генерация диаграмм yEd GraphML | :1347 |
| [`apply_indexed_edits()`](main.py:1479) | Пакетная правка Word по индексам абзацев | :1479 |
| [`smart_excel_search()`](main.py:1526) | Поиск релевантных строк в Excel | :1526 |
| [`smart_excel_edit()`](main.py:1634) | Обновление или добавление строк в Excel | :1634 |
| [`draft_email_tool()`](main.py:1734) | Создание черновика письма в Outlook | :1734 |
| [`draft_meeting_tool()`](main.py:1750) | Создание черновика встречи в Outlook | :1750 |
| [`generate_document_from_template()`](main.py:1769) | Создание документа по шаблону с интеллектуальной заменой | :1769 |
| [`generate_document_from_scratch()`](main.py:1863) | Генерация Word-документа с нуля | :1863 |
| [`generate_excel_from_scratch()`](main.py:1969) | Генерация Excel-таблицы с нуля | :1969 |
| [`load_local_settings()`](main.py:2116) | Загрузка локальных настроек пользователя | :2116 |
| [`save_local_settings()`](main.py:2127) | Сохранение локальных настроек | :2127 |
| [`load_global_settings()`](main.py:2135) | Загрузка глобальных настроек | :2135 |
| [`save_global_settings()`](main.py:2146) | Сохранение глобальных настроек | :2146 |

### 5.2 Методы класса App

| Метод | Описание | Строка |
|-------|----------|--------|
| [`__init__()`](main.py:2170) | Инициализация приложения | :2170 |
| [`fetch_free_models()`](main.py:2293) | Загрузка списка бесплатных моделей из OpenRouter | :2293 |
| [`prompt_auth()`](main.py:2313) | Аутентификация пользователя по паролю | :2313 |
| [`update_ui_for_role()`](main.py:2330) | Обновление UI в соответствии с ролью (Admin/Guest) | :2330 |
| [`ask_save_path_sync()`](main.py:2342) | Диалог сохранения при генерации документов | :2342 |
| [`update_progress_ui()`](main.py:2368) | Обновление прогресс-бара синхронизации | :2368 |
| [`append_to_chat()`](main.py:2376) | Добавление текста в чат | :2376 |
| [`generate_unicode_table()`](main.py:2385) | Рендер Unicode-таблиц в чате | :2385 |
| [`apply_markdown()`](main.py:2473) | Обработка markdown-разметки в ответах | :2473 |
| [`on_link_click()`](main.py:2553) | Обработка клика по ссылке на файл | :2553 |
| [`load_history()`](main.py:2585) | Загрузка истории чата | :2585 |
| [`save_history()`](main.py:2592) | Сохранение истории чата | :2592 |
| [`generate_session_title_background()`](main.py:2598) | Автогенерация заголовка сессии в фоне | :2598 |
| [`save_current_session()`](main.py:2616) | Сохранение текущей сессии в JSON | :2616 |
| [`load_session()`](main.py:2641) | Загрузка сессии по ID | :2641 |
| [`open_history_window()`](main.py:2630) | Открытие окна истории диалогов | :2630 |
| [`run_background_secretary()`](main.py:2752) | Фоновый анализ диалога и запоминание фактов | :2752 |
| [`clear_chat()`](main.py:2790) | Очистка чата | :2790 |
| [`manual_sync()`](main.py:2809) | Ручной запуск синхронизации | :2809 |
| [`open_settings()`](main.py:2824) | Открытие окна настроек с вкладками | :2824 |
| [`get_tools_schema()`](main.py:3120) | Определение схемы инструментов для LLM | :3120 |
| [`execute_tool()`](main.py:3453) | Маршрутизация вызовов инструментов | :3453 |
| [`send_message()`](main.py:3487) | Отправка сообщения пользователя | :3487 |
| [`agent_loop()`](main.py:3500) | Основной агентный цикл обработки сообщений | :3500 |

Эти утилиты используются внутренними механизмами приложения и обеспечивают работу основных инструментов агента.

---

## 6) Зависимости

Текущие импорты проекта требуют следующие библиотеки:

- `chromadb`
- `python-docx`
- `python-dotenv`
- `openai`
- `customtkinter`
- `keyboard`
- `openpyxl`
- `pywin32`
- `PyMuPDF`
- `requests` — для работы с Tavily API и OpenRouter
- `wikipedia` — для поиска в Википедии
- `cryptography` — для Vault-шифрования (Fernet)
- `xml.etree.ElementTree` — встроенный модуль для парсинга GraphML
- `ffmpeg` / `ffprobe` — внешние утилиты для нарезки аудиофайлов

### 6.1 Рекомендуемый `requirements.txt`

```
chromadb
python-docx
python-dotenv
openai
customtkinter
keyboard
openpyxl
pywin32
PyMuPDF
requests
wikipedia
cryptography
```

> **Внешние зависимости:**
> - Для COM-сценариев Word и Outlook необходим установленный Microsoft Office/Outlook с доступными COM-объектами в среде текущего пользователя Windows.
> - Для транскрибации аудио необходимы утилиты `ffmpeg` и `ffprobe`, доступные в PATH системы.

---

## 7) Установка и запуск

### 7.1 Предварительные требования

- Windows 10/11;
- Python 3.10+;
- доступ в интернет для OpenRouter API;
- установленный Microsoft Word/Outlook для COM-функций;
- `ffmpeg` и `ffprobe` в PATH системы (для транскрибации аудио).

### 7.2 Установка

```cmd
python -m venv .venv
.venv\Scripts\activate
pip install --upgrade pip
pip install chromadb python-docx python-dotenv openai customtkinter keyboard openpyxl pywin32 PyMuPDF requests wikipedia cryptography
```

### 7.3 Настройка окружения

В [`.env`](.env) должен быть минимум один ключ:

```env
OPENROUTER_API_KEY=ваш_ключ
```

### 7.4 Запуск

Вариант 1:

```cmd
start.bat
```

Вариант 2:

```bash
python main.py
```

При запуске приложение автоматически:

- создает каталоги `Memory` и `SMK_Docs`, если их нет;
- запускает фоновую синхронизацию базы знаний;
- загружает список бесплатных моделей из OpenRouter;
- отображает статус готовности базы в интерфейсе.

---

## 8) Конфигурация

### 8.1 Файл настроек

Конфигурация хранится в [`global_settings.json`](global_settings.json). Ключевые параметры:

- `vision_model` — модель для Vision OCR (используется в экспериментальном контуре);
- `secretary_model` — модель фонового секретаря;
- `embedding_model` — модель облачных эмбеддингов;
- `audio_provider` — провайдер аудио-транскрибации (OpenRouter/Groq);
- `audio_model` — модель для аудио-транскрибации;
- `audio_chunk_mins` — длительность одного аудио-фрагмента (мин);
- `audio_overlap_secs` — перекрытие между аудио-фрагментами (сек);
- `indexed_folders` — массив путей к папкам для индексации (поддержка сетевых путей, например `Z:/...`);
- `exclude_keywords` — массив ключевых слов для исключения файлов из индексации (например, «архив», «not_index», «черновик»);
- `default_excel_file` — Excel-файл по умолчанию;
- `excel_status_col` — колонка статуса в Excel;
- `excel_open_val` / `excel_closed_val` — допустимые значения статусов;
- `chroma_batch_size` — размер батча при индексации.

#### Локальные настройки (local_settings.json)

| Параметр | Описание |
|----------|----------|
| `guest_model` | Модель для режима Guest (бесплатная) |
| `admin_model` | Модель для режима Admin |
| `model_history` | История использованных моделей (Топ-10) |
| `use_proxy` | Флаг использования SOCKS5-прокси |
| `proxy_host` | Хост прокси (по умолчанию 127.0.0.1) |
| `proxy_port` | Порт прокси (по умолчанию 2080) |

#### Зашифрованный Vault (secrets.vault)

| Параметр | Описание |
|----------|----------|
| `openrouter_key` | API-ключ OpenRouter |
| `groq_key` | API-ключ Groq (для аудио) |
| `tavily_key` | API-ключ Tavily (для поиска в интернете) |
| `admin_password` | Пароль администратора |

### 8.2 GUI-настройки

Через окно настроек [`App.open_settings()`](main.py:2824) с вкладками можно настроить:

#### Вкладка «Модели»
- модель агента (Admin/Guest);
- модель для Vision OCR;
- модель фонового секретаря;
- модель эмбеддингов (требует перезапуска).

#### Вкладка «Исключения»
- ключевые слова для исключения файлов/папок из индексации.

#### Вкладка «Папки»
- добавление и удаление папок для индексации (только Admin).

#### Вкладка «Безопасность» (только Admin)
- OpenRouter API Key;
- Groq API Key;
- Tavily API Key;
- Пароль администратора.

#### Вкладка «Аудио и Сеть» (только Admin)
- аудио-провайдер (OpenRouter/Groq);
- аудио-модель (с автозагрузкой доступных моделей);
- длительность куска (мин) и перекрытие (сек);
- использование SOCKS5-прокси (host/port).

#### Вкладка «О программе»
- информация о версии, разработчике и контактах.

Изменение `embedding_model` требует перезапуска приложения, поскольку эмбеддинг-функция инициализируется при старте.
